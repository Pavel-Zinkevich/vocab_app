name: vocab_app
description: A simple vocabulary app with 3 tabs.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^7.2.0
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  html_unescape: ^2.0.0
  html: ^0.15.0
  just_audio: ^0.9.40
  shared_preferences: ^2.2.2

  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  # cloud_firestore: ^5.6.12

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true